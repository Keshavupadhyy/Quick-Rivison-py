
# Tuples in Python are similar to lists but with some key differences: they are immutable, meaning their contents cannot be changed after they are created.
# Tuples are typically used to store a collection of items that should not be modified throughout the program.


t = (2,3,4,5,6,7,8,9,1,1,2,2)
print(t.count(1))
print(t.index(1))

